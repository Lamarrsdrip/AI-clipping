{
  "name": "clipforge-ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI clipping SaaS MVP for owned or permissioned YouTube videos.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "lint": "node --check server.js && node --check public/app.js && python3 -m py_compile face_track.py",
    "typecheck": "npm run lint",
    "build": "npm run lint",
    "test": "NODE_ENV=test node --test"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "busboy": "^1.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
